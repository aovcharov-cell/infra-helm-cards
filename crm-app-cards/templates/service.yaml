apiVersion: v1
kind: Service
metadata:
  name: {{ include "crm-app-cards.fullname" . }}
  labels:
    {{- include "crm-app-cards.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crm-app-cards.selectorLabels" . | nindent 4 }}
