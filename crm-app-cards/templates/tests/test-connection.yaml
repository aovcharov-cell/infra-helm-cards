apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crm-app-cards.fullname" . }}-test-connection"
  labels:
    {{- include "crm-app-cards.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crm-app-cards.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
