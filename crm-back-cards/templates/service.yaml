apiVersion: v1
kind: Service
metadata:
  name: {{ include "crm-back-cards.fullname" . }}
  labels:
    {{- include "crm-back-cards.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crm-back-cards.selectorLabels" . | nindent 4 }}
